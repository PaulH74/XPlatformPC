namespace KanePC
{
    /// <summary>
    /// Public enum used to maintain a centralised, common population of selectable tools.
    /// </summary>
    public enum ToolBox
    {
        LASER_POINTER
    }
}